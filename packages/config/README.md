# Shared configuration

This workspace reserves a home for reusable build, lint, and deployment configuration as the monorepo grows. Root ESLint, Prettier, and TypeScript configuration are active during Phase 1.
